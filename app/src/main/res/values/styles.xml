<resources>

    <!-- Base application theme. -->
    <style name="buzzinessCard." parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="buzzinessCard" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorAccent">@color/accent_material_light_1</item>
        <item name="android:colorForeground">@android:color/background_dark</item>
        <item name="android:textColorSecondary">@color/rect_stroke_solid</item>
    </style>

</resources>
